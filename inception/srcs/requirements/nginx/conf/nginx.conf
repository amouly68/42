# 6500 a changer par 80
# 6656 a changer par 443
 

server {
    listen 80;
    listen [::]:80;
#     listen 6500;
#     listen [::]:6500;
    server_name amouly.42.fr;
    return 301 https://$host$request_uri;
}

server {
        listen 443 ssl;
        listen [::]:443 ssl;
        server_name $DOMAIN_NAME;

        root /var/www/html;
        index index.html index.htm index.php;

        # Chemins des certificats SSL/TLS générés dans le conteneur
        ssl_certificate /etc/nginx/ssl/mon_site.crt;
        ssl_certificate_key /etc/nginx/ssl/mon_site.key;

        # Configuration TLS spécifique
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers 'TLS_AES_128_GCM_SHA256:TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384';

        location / {
                try_files $uri $uri/ /index.php?$args;
        }


        location ~ [^/]\.php(/|$) { 
                try_files $uri =404;
                fastcgi_pass wordpress:9000;
                fastcgi_index index.php;
                include fastcgi_params;
                fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        }
}

