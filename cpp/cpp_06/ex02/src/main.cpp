

#include "Base.hpp"
#include <iostream>




int main()
{
    Base* test = NULL;
    test = test->generate();
    test->identify(test);
    test = test->generate();
    test->identify(test);
    test = test->generate();
    test->identify(test);
    test = test->generate();
    test->identify(test);
    test = test->generate();
    test->identify(test);

}